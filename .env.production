# Production environment variables
# These will be used when NODE_ENV=production

# Application URL for email verification links and other absolute URLs
NEXT_PUBLIC_APP_URL=firebase-auth-git-main-namokar100s-projects.vercel.app

# Note: Other environment variables will be provided through the Vercel dashboard
# or inherited from .env.local during the build process 